# Backend environment variables
PORT=8000
HOST=0.0.0.0
ENVIRONMENT=development
# Replace with actual keys in production
MODEL_CHECKPOINT_DIR=./data/models
TELEMETRY_STREAM_URL=wss://source.isro.gov.in/telemetry
